{
  "name": "perplexity-ai-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "A full-stack AI research workspace with persistent conversations and email authentication.",
  "scripts": {
    "install:all": "npm install --prefix Backend && npm install --prefix frontend/project0",
    "dev:backend": "npm run dev --prefix Backend",
    "dev:frontend": "npm run dev --prefix frontend/project0",
    "start": "npm start --prefix Backend",
    "build": "npm run build --prefix frontend/project0",
    "lint": "npm run lint --prefix frontend/project0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
